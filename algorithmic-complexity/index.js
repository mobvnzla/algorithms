/* 
Temporary Complexity

let bar = 'test'	// O(1)
if() {}		// O(1)
for() {}		// O(n)
while() {}	// O(n)
for() { for() {} }// O(n^2) 

Spatial Complexity

let bar = 'test'  // 0(1)
if () {}               // 0(1)
for () {}             // 0(1)
let result = [1,2,...,n]  // 0(n)
let two-dimensional = [[2,4],[6,8],[10,12]] //0(n^2)

and keep it simple

O(2n) ----> O(n)

O(50) ----> O(k) or O(1)

O(m**2 + 50) ----> O(n**2)

*/
